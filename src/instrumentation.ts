export async function register() {
  // This file intentionally left minimal — no heavy imports here.
  // Production seed copy is handled by railway.toml startCommand.
  // Payload migrations run automatically via `prodMigrations` on first
  // getPayload() call inside each server page.
}
